/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'sl.ticketm.net',
            's1.ticketm.net',
            'images.universe.com',
        ],
    },
}

export default nextConfig